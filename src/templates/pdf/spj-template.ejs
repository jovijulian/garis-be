<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Surat Perintah Jalan - Request #<%= request.id %></title>
    <style>

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-print-color-adjust: exact; 
            print-color-adjust: exact; 
        }

        @page {
            size: A4;
            margin: 20px;
        }
        @media print {
          body {
              -webkit-print-color-adjust: exact;
          }
          /* Sembunyikan elemen yang tidak perlu dicetak jika ada */
          .no-print {
              display: none;
          }
      }
          body {
            font-family: "Times New Roman", Times, serif;
            background: white;
            color: #000;
            line-height: 1.4; 
            font-size: 11pt; 
          }
          
          .container {
            max-width: 100%;
            margin: 0 auto;
            background: white;
          }
          
          .letterhead {
            border-bottom: 2px solid #000; 
            padding-bottom: 10px;
            margin-bottom: 20px; 
          }
          
          .letterhead-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
          }
          
          .company-info {
            flex: 1;
          }
          
          .company-name {
            font-size: 16pt; 
            font-weight: bold;
            color: #000;
            margin-bottom: 3px; 
            text-transform: uppercase;
            letter-spacing: 1px;
          }
          
          .company-address {
            font-size: 9pt;
            line-height: 1.4; 
            color: #333;
          }
          
          .logo-placeholder {
            width: 100px;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 9pt; /* Smaller text */
          }
          .logo-placeholder img {
            max-height: 70px !important;
            width: auto !important;
          }
          
          .document-header {
            text-align: center;
            margin: 20px 0; 
            padding: 10px 0;
          }
          
          .document-title {
            font-size: 14pt; 
            font-weight: bold;
            text-decoration: underline;
            margin-bottom: 5px;
            letter-spacing: 1px; 
          }
          
          .document-number {
            font-size: 10pt; 
            margin-top: 5px;
          }
          
          .section {
            margin-bottom: 15px; 
          }
          
          .section-title {
            font-size: 11pt; 
            font-weight: bold;
            margin-bottom: 10px; 
            text-transform: uppercase;
            border-bottom: 1px solid #000; 
            padding-bottom: 3px; 
          }
          
          .detail-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px; 
          }
          
          .detail-table td {
            padding: 4px 6px; 
            vertical-align: top;
            border-bottom: 1px dotted #ccc;
            font-size: 10pt; 
          }
          
          .detail-table td:first-child {
            width: 180px; 
            font-weight: bold;
          }
          
          .detail-table td:nth-child(2) {
            width: 15px; 
            text-align: center;
          }
          
          .detail-table tr:last-child td {
            border-bottom: none;
          }
          
      
          .assignment-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px; 
            border: 1px solid #000;
          }
          
          .assignment-table th,
          .assignment-table td {
            border: 1px solid #000;
            padding: 5px 8px;
            text-align: left;
          }
          
          .assignment-table th {
            background-color: #f0f0f0;
            font-weight: bold;
            text-align: center;
            font-size: 10pt; 
          }
          
          .assignment-table td {
            font-size: 10pt; 
          }
          
          .no-data {
            text-align: center;
            padding: 15px; 
            font-style: italic;
            color: #666;
            font-size: 10pt;
          }
          
          /* Notes Box */
          .notes-box {
            border: 1px solid #ccc;
            padding: 10px; 
            margin-top: 8px;
            background-color: #f9f9f9;
            font-size: 10pt;
          }
          
          .notes-box .label {
            font-weight: bold;
            margin-bottom: 5px;
          }
          
          .signature-section {
            margin-top: 15px;
            margin-bottom: 15px;
            font-size: 10pt;
          }
          
          .footer {
            margin-top: 30px;
            padding-top: 10px;
            border-top: 1px solid #eee;
            overflow: hidden;
          }
          .signature {
            float: right;
            width: 220px;
            text-align: center;
          }
          .signature p {
            margin: 0 0 3px 0;
            font-size: 10pt; 
          }
          .signature .date {
            margin-bottom: 40px;
          }
          .signature .name-placeholder {
            margin-bottom: 3px; 
            font-weight: bold;
          }
          .app-footer {
            flex-shrink: 0;
            text-align: center;
            font-size: 8pt;
            margin-top: 10px; 
            clear: both;
          }
          .footer-brand {
            font-size: 8pt; 
            color: #888;
          }
          
          @media print {
            body {
              padding: 0;
            }
            .container {
              padding: 0;
            }
            .section {
              page-break-inside: avoid;
            }
          }
          
    </style>
</head>
<body>
    <div class="container">
        <!-- Kop Surat -->
        <div class="letterhead">
            <div class="letterhead-content">
                <div class="company-info">
                    <div class="company-name">PT. CISANGKAN</div>
                    <div class="company-address">
                        Jl. Haji Alpi No 107, Cijerah, Bandung 40221<br>
                        Telp: (022) 6031588 | Fax: (022) 6030467<br>
                        Email: info@cisangkan.com | Website: www.cisangkan.co.id
                    </div>
                </div>
                <div class="logo-placeholder">
                    <% if (logoBase64) { %>
                        <img src="data:image/png;base64,<%= logoBase64 %>" alt="Logo PT. Cisangkan" style="max-height: 80px; width: auto;">
                    <% } else { %>
                        <span>LOGO</span>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Document Title -->
        <div class="document-header">
            <div class="document-title">SURAT PERINTAH JALAN</div>
            <div class="document-number">Nomor: SPJ/<%= request.id %>/<%= moment().format('MM/YYYY') %></div>
        </div>

        <!-- Request Details -->
        <div class="section">
            <div class="section-title">I. DATA PERMINTAAN</div>
            <table class="detail-table">
                <tr>
                    <td>Nama Pemohon</td>
                    <td>:</td>
                    <td><%= request.user?.nama_user || '-' %></td>
                </tr>
                <tr>
                    <td>Keperluan</td>
                    <td>:</td>
                    <td><%= request.purpose %></td>
                </tr>
                <tr>
                    <td>Tujuan</td>
                    <td>:</td>
                    <td><%= request.destination %></td>
                </tr>
                <tr>
                    <td>Lokasi Penjemputan</td>
                    <td>:</td>
                    <td><%= request.pickup_location_text || request.cabang?.nama_cab || '-' %></td>
                </tr>
                <tr>
                    <td>Tanggal & Waktu Keberangkatan</td>
                    <td>:</td>
                    <td><%= moment(request.start_time).format('dddd, DD MMMM YYYY') %> Pukul <%= moment(request.start_time).add(7, 'hours').format('HH:mm') %> WIB</td>
                </tr>
                <tr>
                    <td>Tanggal & Waktu Kembali</td>
                    <td>:</td>
                    <td><%= request.end_time ? moment(request.end_time).format('dddd, DD MMMM YYYY') + ' Pukul ' + moment(request.end_time).add(7, 'hours').format('HH:mm') + ' WIB' : 'Belum Ditentukan' %></td>
                </tr>
                <tr>
                    <td>Jumlah Penumpang</td>
                    <td>:</td>
                    <td><%= request.passenger_count %> (orang)</td>
                </tr>
                <tr>
                    <td>Nama-nama Penumpang</td>
                    <td>:</td>
                    <td><%= request.passenger_names || 'Tidak Disebutkan' %></td>
                </tr>
            </table>
            
            <% if (request.note) { %>
            <div class="notes-box">
                <div class="label">Catatan Khusus:</div>
                <div><%= request.note %></div>
            </div>
            <% } %>
        </div>

        <!-- Assignment Details -->
        <div class="section">
            <div class="section-title">II. PENUGASAN KENDARAAN DAN PENGEMUDI</div>
            
            <% if (request.detail && request.detail.length > 0) { %>
            <table class="assignment-table">
                <thead>
                    <tr>
                        <th style="width: 5%;">No</th>
                        <th style="width: 25%;">Jenis Kendaraan</th>
                        <th style="width: 15%;">No. Polisi</th>
                        <th style="width: 20%;">Nama Pengemudi</th>
                        <th style="width: 35%;">Catatan</th>
                    </tr>
                </thead>
                <tbody>
                    <% request.detail.forEach(function(assignment, index){ %>
                    <tr>
                        <td style="text-align: center;"><%= index + 1 %></td>
                        <td><%= assignment.vehicle?.name || '-' %></td>
                        <td style="text-align: center; font-weight: bold;"><%= assignment.vehicle?.license_plate || '-' %></td>
                        <td><%= assignment.driver?.name || 'Belum Ditugaskan' %></td>
                        <td><%= assignment.note_for_driver || '-' %></td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
            <% } else { %>
            <div class="notes-box">
                <div class="no-data">Belum ada penugasan kendaraan dan pengemudi</div>
            </div>
            <% } %>
        </div>

        <!-- Signature Section -->
        <div class="signature-section">
            <div style="text-align: left; margin-bottom: 10px;">
                Demikian Surat Perintah Jalan ini dibuat untuk dapat dilaksanakan dengan sebaik-baiknya.
            </div>
        </div>

        <!-- Document Footer -->
        <div class="footer">
            <div class="signature">
                <h4 class="date"><%= request.cabang?.nama_cab || 'Kantor Pusat' %>, <%= moment().format('DD MMMM YYYY') %></h4>
                <br/>
                <br/>
                <h4 class="name-placeholder">(_________________________)</h4>
            </div>
        </div>
        <div class="app-footer">
            <p class="footer-brand">Generated by GARIS</p>
          </div>
    </div>
</body>
</html>